package pl.edu.pja.s28687.logistics;

import pl.edu.pja.s28687.Locomotive;

public class DestinationSwitch {

    public static void switchDestination(Locomotive loc, LocoBase locoBase){
//        loc.setDestStation(locoBase.getTrainStations().stream().filter(station -> NaiveRouteFinder.findRoute(loc.getLastTrainStation(), station, locoBase).isPresent()).findFirst().get());

    }
}
